import type { Paper,PaperCategory } from "../src/types";
export function abstractText(index:Record<string,number[]>|null|undefined){if(!index)return null;const words:string[]=[];for(const [word,positions] of Object.entries(index))for(const position of positions)words[position]=word;return words.join(" ")||null}
export function mapWork(work:any,category:PaperCategory):Paper {const doi=typeof work.doi==="string"?work.doi.replace(/^https?:\/\/(dx\.)?doi\.org\//i,""):null;const optionalString=(value:unknown)=>typeof value==="string"&&value.trim()?value:null;return {id:String(work.id||""),title:String(work.title||work.display_name||"Untitled"),authors:Array.isArray(work.authorships)?work.authorships.map((a:any)=>a.author?.display_name).filter(Boolean):[],year:Number.isInteger(work.publication_year)?work.publication_year:null,abstract:abstractText(work.abstract_inverted_index),topics:Array.isArray(work.topics)?work.topics.slice(0,5).map((t:any)=>({id:String(t.id||""),name:String(t.display_name||"")})):[],doi,landingPageUrl:work.primary_location?.landing_page_url||work.id||null,openAccessUrl:work.best_oa_location?.pdf_url||work.best_oa_location?.landing_page_url||null,citedByCount:Number(work.cited_by_count)||0,category,venue:optionalString(work.primary_location?.source?.display_name),volume:optionalString(work.biblio?.volume),issue:optionalString(work.biblio?.issue),firstPage:optionalString(work.biblio?.first_page),lastPage:optionalString(work.biblio?.last_page),publicationDate:optionalString(work.publication_date)}}
