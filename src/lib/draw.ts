import type { GachaCandidates, GachaSettings, Paper, PaperCategory, SelectedTopic } from "../types";
const shuffle=<T,>(a:T[])=>{const out=[...a];for(let i=out.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[out[i],out[j]]=[out[j],out[i]]}return out};
const config:Record<PaperCategory,{topics:keyof GachaSettings;count:keyof GachaSettings}>={expert:{topics:"expertTopics",count:"expertCount"},related:{topics:"relatedTopics",count:"relatedCount"},other:{topics:"otherTopics",count:"otherCount"}};
export function drawPapers(candidates:GachaCandidates,settings:GachaSettings,excluded:Set<string>){const result:Paper[]=[];const used=new Set(excluded);for(const category of Object.keys(config) as PaperCategory[]){const {topics,count}=config[category];const selected=settings[topics] as SelectedTopic[];const needed=settings[count] as number;const queues=selected.map(t=>shuffle(candidates[category]?.[t.id]||[]));while(result.filter(p=>p.category===category).length<needed){let added=false;for(const queue of queues){const paper=queue.find(p=>!used.has(p.id));if(paper){used.add(paper.id);result.push({...paper,category});queue.splice(queue.indexOf(paper),1);added=true;if(result.filter(p=>p.category===category).length===needed)break}}if(!added)break;} }return result;}
